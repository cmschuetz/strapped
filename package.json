{
  "name": "strapped",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun tools/build.ts",
    "test": "claude plugin validate . --strict && claude plugin validate plugins/strapped --strict && bun run typecheck && bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
